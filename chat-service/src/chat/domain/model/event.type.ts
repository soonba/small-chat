export enum EventType {
  SUBSCRIBE = 'subscribe',
  UN_SUBSCRIBE = 'unSubscribe',
  MESSAGE = 'message',
}

export enum PropertyKey {
  CHAT_IDS = 'chatIds',
  CHAT_ID = 'chatId',
  MESSAGE = 'messageBody',
}
